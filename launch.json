{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Executar OpenGL 64-bit",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main.exe",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "miDebuggerPath": "C:/MinGW/bin/gdb.exe", // ou o caminho onde está seu gdb
            "stopAtEntry": false
        }
    ]
}
